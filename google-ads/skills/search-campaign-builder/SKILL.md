---
name: search-campaign-builder
description: "Google Ads Search Campaign builder voor lead generation. Gebruik voor: (1) RSA best practices en ad strength optimalisatie, (2) Ad extensions setup (sitelinks, callouts, call), (3) Search campaign structuur, (4) Lead gen keyword strategie, (5) Conversie tracking voor leads, (6) Call tracking en form submissions. Triggers: search campaign, rsa, responsive search ads, ad strength, extensions, sitelinks, callouts, lead generation, call extension, lead forms."
---

# Search Campaign Builder

Complete gids voor het opzetten van high-performance Google Ads Search campaigns specifiek voor lead generation met RSA best practices en volledige ad extensions setup.

## Quick Decision Guide

```
WELK CAMPAGNE TYPE PAST BIJ JOU?
│
├─► B2B LEAD GENERATION
│   └─► SEARCH + PERFORMANCE MAX
│       ├── Search voor high-intent queries
│       ├── PMax voor Display/Discovery reach
│       └── Focus op qualified leads, niet volume
│
├─► LOCAL SERVICE BUSINESS
│   └─► SEARCH + LOCAL SERVICES ADS
│       ├── Locatie-based targeting
│       ├── Call extensions prominent
│       └── Google Guaranteed badge (indien mogelijk)
│
├─► HIGH-VALUE LEADS (Consult, Demo)
│   └─► SEARCH-ONLY CAMPAIGN
│       ├── Exact/Phrase match focus
│       ├── Premium positionering
│       └── Strict qualification in ads
│
└─► VOLUME LEAD GENERATION
    └─► SEARCH + LEAD FORM EXTENSIONS
        ├── Lower friction conversie
        ├── Native Google forms
        └── CRM integratie
```

## Campaign Structuur voor Lead Gen

### Aanbevolen Account Structuur

```
LEAD GEN ACCOUNT STRUCTUUR
══════════════════════════

CAMPAIGN 1: Brand
├── Ad Group: Brand Exact
│   ├── [bedrijfsnaam]
│   ├── [merk + service]
│   └── High bid, maximize conversions
├── Budget: 10-15% of total
└── Doel: Capture branded intent

CAMPAIGN 2: High-Intent Non-Brand
├── Ad Group: Service + Action
│   ├── "[service] aanvragen"
│   ├── "[service] offerte"
│   └── "[service] contact"
├── Ad Group: Service + Urgency
│   ├── "[service] vandaag"
│   ├── "[service] direct"
│   └── "[service] nu"
├── Budget: 50-60% of total
└── Doel: Qualified leads

CAMPAIGN 3: Research/Consideration
├── Ad Group: Service + Info
│   ├── "beste [service]"
│   ├── "[service] vergelijken"
│   └── "wat kost [service]"
├── Budget: 20-25% of total
└── Doel: Top-funnel leads

CAMPAIGN 4: Competitor
├── Ad Group: Competitor Names
│   ├── [competitor 1]
│   ├── [competitor 2] alternatief
│   └── Strict negatives
├── Budget: 5-10% of total
└── Doel: Competitive conquest
```

### Ad Group Best Practices

```
AD GROUP SETUP VOOR LEAD GEN
════════════════════════════

KEYWORDS PER AD GROUP:
──────────────────────
□ 10-20 keywords per ad group (STAG approach)
□ Zelfde intent cluster
□ Zelfde landing page
□ Zelfde ad copy theme

MATCH TYPE STRATEGIE (Lead Gen):
────────────────────────────────
┌─────────────────────────────────────────────────────────┐
│ Match Type │ Budget % │ Use Case                        │
├────────────┼──────────┼─────────────────────────────────┤
│ Exact      │ 40-50%   │ Proven converting queries       │
│ Phrase     │ 30-40%   │ Core service terms              │
│ Broad      │ 10-20%   │ Discovery + Smart Bidding only  │
└────────────┴──────────┴─────────────────────────────────┘

⚠️ LEAD GEN SPECIFIEK:
├── Broad match ALLEEN met tCPA bidding
├── Veel negatives voor kwaliteit
└── Focus op qualified leads, niet CTR
```

## RSA Best Practices

### Responsive Search Ad Structuur

```
RSA ANATOMIE (2025)
═══════════════════

HEADLINES (Max 15, Min 8):
──────────────────────────
Position 1 Headlines (Pin 2-3):
├── [Keyword] - [Primaire Benefit]
├── [Service] Aanvragen | Direct Contact
└── "[Keyword]" + CTA

Position 2 Headlines (Pin 1-2):
├── [Belangrijkste USP]
├── Gratis [Offerte/Consult/Demo]
└── [Social Proof]

Position 3+ Headlines (No Pin, laat testen):
├── Benefit statements
├── Trust signals
├── Urgency (waar gepast)
├── Locatie specifiek
└── Variaties op CTA

DESCRIPTIONS (Max 4, Min 3):
────────────────────────────
Description 1 (Pin to position 1):
├── Keyword + primaire value prop
├── Call-to-action
└── Max 90 chars voor visibility

Description 2-4 (Geen pinning):
├── Social proof / testimonials
├── Proces uitleg
├── Trust signals
└── Secondary benefits

AD STRENGTH TARGETS:
────────────────────
□ "Excellent" = geen garantie op betere performance
□ "Good" = voldoende, focus op conversies
□ Minimaal "Average" = onder dit niveau actie vereist
```

### Ad Strength Verbeteren

```
AD STRENGTH OPTIMALISATIE
═════════════════════════

"POOR" OF "AVERAGE" AD STRENGTH? CHECK:
───────────────────────────────────────

1. HEADLINE DIVERSITEIT
   □ Gebruik alle 15 headline slots
   □ Varieer zinstructuur
   □ Mix keywords, benefits, CTAs
   □ Vermijd repetitie

2. DESCRIPTION DIVERSITEIT
   □ Gebruik alle 4 description slots
   □ Elk unieke boodschap
   □ Verschillende lengtes

3. KEYWORD INCLUSIE
   □ Keywords in min 3 headlines
   □ Keywords in min 1 description
   □ Natural fit, geen keyword stuffing

4. UNIEKE SELLING POINTS
   □ Min 5 unieke USPs verwerkt
   □ Concrete cijfers (30% korting, 1000+ klanten)
   □ Differentiators vs competitors

SNEL AD STRENGTH VERHOGEN:
──────────────────────────
□ Voeg "popular" headlines toe die Google suggereert
□ Include prijzen/getallen
□ Voeg locatie headlines toe
□ Mix korte en lange headlines
```

### Lead Gen Ad Copy Templates

```
HEADLINE TEMPLATES (Lead Gen)
═════════════════════════════

KEYWORD-FOCUSED:
├── {KeyWord:Service} Aanvragen
├── {KeyWord:Service} - Vraag Offerte
├── Zoekt u [Service]?
└── [Service] Specialist in [Regio]

BENEFIT-FOCUSED:
├── Gratis Offerte Binnen 24 Uur
├── Geen Verplichtingen
├── Persoonlijk Advies op Maat
└── Direct Antwoord op Uw Vraag

TRUST-FOCUSED:
├── 15+ Jaar Ervaring
├── 4.9★ Google Reviews
├── 1000+ Tevreden Klanten
└── Gecertificeerd [Certificaat]

URGENCY-FOCUSED:
├── Vandaag Nog Reactie
├── Plan Direct Uw Afspraak
├── Start Vandaag
└── Binnen 1 Uur Contact

CTA-FOCUSED:
├── Bel Nu: [Nummer]
├── Vraag Gratis Advies
├── Plan Uw Consult
└── Download Brochure

DESCRIPTION TEMPLATES:
──────────────────────
Template 1 (Keyword + Benefit + CTA):
"Op zoek naar [service]? Wij bieden [benefit] met [USP].
Vraag vandaag nog uw gratis offerte aan."

Template 2 (Trust + Process):
"Al 15 jaar expert in [service]. Persoonlijke aanpak,
duidelijke prijzen, geen verrassingen. Neem contact op."

Template 3 (USP + Social Proof):
"[Aantal]+ tevreden klanten gingen u voor. [USP 1],
[USP 2] en [USP 3]. Plan uw gratis consult."

Template 4 (Problem + Solution):
"[Probleem]? Wij helpen u met [oplossing]. Expert advies,
snelle service, resultaatgarantie."
```

## Ad Extensions Setup

### Verplichte Extensions voor Lead Gen

```
AD EXTENSIONS CHECKLIST (Lead Gen)
══════════════════════════════════

1. SITELINK EXTENSIONS [VERPLICHT]
──────────────────────────────────
Min 4 sitelinks (max 8 voor desktop):

Lead Gen Sitelinks:
├── "Vraag Offerte Aan" → /offerte
├── "Bel Ons Direct" → /contact (met telefoonnummer)
├── "Onze Diensten" → /diensten
├── "Over Ons" → /over-ons
├── "Reviews & Referenties" → /reviews
├── "Gratis Adviesgesprek" → /advies
├── "FAQ / Veelgestelde Vragen" → /faq
└── "Locaties" → /locaties

Sitelink Best Practices:
□ 25+ karakters descriptions
□ Unieke landing pages
□ Mobile-first thinking
□ Track sitelink conversions

2. CALLOUT EXTENSIONS [VERPLICHT]
─────────────────────────────────
Min 4 callouts (max 10):

Voorbeelden:
├── "Gratis Offerte"
├── "Binnen 24 Uur Reactie"
├── "Geen Voorrijkosten"
├── "Gecertificeerd"
├── "15+ Jaar Ervaring"
├── "Persoonlijk Advies"
├── "Maatwerk Oplossingen"
├── "Flexibele Tijden"
├── "All-in Prijzen"
└── "Tevredenheidsgarantie"

3. STRUCTURED SNIPPETS [VERPLICHT]
──────────────────────────────────
Kies relevante headers:

Diensten: [dienst 1], [dienst 2], [dienst 3]
Types: [type 1], [type 2], [type 3]
Merken: [merk 1], [merk 2], [merk 3] (indien dealer)

4. CALL EXTENSIONS [KRITIEK VOOR LEAD GEN]
──────────────────────────────────────────
□ Hoofd telefoonnummer
□ Call tracking nummer (via Google Ads of third party)
□ Schema: Alleen tonen tijdens openingstijden
□ Mobile: Click-to-call prominent

5. LEAD FORM EXTENSIONS [OPTIONEEL]
───────────────────────────────────
Native Google lead forms:
├── Lage friction (geen landing page nodig)
├── Pre-filled met Google account data
├── Direct in search results
├── CRM integratie (Zapier, native)

⚠️ Trade-off: Lagere lead kwaliteit vs meer volume
```

### Geavanceerde Extension Setup

```
GEAVANCEERDE EXTENSIONS
═══════════════════════

6. LOCATION EXTENSIONS
──────────────────────
□ Link Google Business Profile
□ Afstand tonen in ads
□ Driving directions
□ Store visits tracking

7. PRICE EXTENSIONS
───────────────────
Ideaal voor diensten met vaste prijzen:
├── "Consult" - "Vanaf €99"
├── "Basis Pakket" - "€199/maand"
├── "Premium Service" - "€499"
└── Link naar relevante pagina's

8. IMAGE EXTENSIONS
───────────────────
□ Relevant product/service beeld
□ 1.91:1 aspect ratio
□ High quality, geen tekst in beeld
□ A/B test verschillende beelden

9. BUSINESS NAME & LOGO
───────────────────────
□ Automatisch uit Business Profile
□ Zorg voor correcte branding
□ Logo voldoet aan richtlijnen

EXTENSION SCHEDULING:
─────────────────────
□ Call extensions: Alleen openingstijden
□ Sitelinks "Bel Nu": Alleen kantoortijden
□ Urgency callouts: Specifieke dagen/tijden
```

## Lead Form Extensions Setup

### Native Lead Forms Configuratie

```
LEAD FORM EXTENSION SETUP
═════════════════════════

WANNEER GEBRUIKEN:
──────────────────
✓ Volume belangrijker dan kwaliteit
✓ Simple lead capture (naam, email, telefoon)
✓ Geen complex qualification nodig
✓ Mobile-heavy traffic

WANNEER NIET GEBRUIKEN:
───────────────────────
✗ Complex B2B met qualification vragen
✗ High-value leads die nurturing nodig hebben
✗ Wanneer landing page deel is van sales process

SETUP STAPPEN:
──────────────
1. Campaign → Extensions → Lead Form
2. Kies form type:
   ├── More leads: Kortere form, meer volume
   └── More qualified: Langere form, betere kwaliteit

3. Configureer velden:
   STANDAARD VELDEN:
   ├── Naam (altijd aan)
   ├── Email (altijd aan)
   ├── Telefoonnummer (optioneel)
   └── Stad/Postcode (optioneel)

   CUSTOM VRAGEN (max 5):
   ├── "Wanneer heeft u deze service nodig?"
   │   └── Opties: Direct, Binnen 1 maand, 1-3 maanden, Later
   ├── "Wat is uw budget?"
   │   └── Opties: €1k-5k, €5k-10k, €10k+, Onbekend
   └── "Hoe kunnen wij u helpen?"
       └── Open tekstveld

4. Headline & Description:
   └── "Ontvang gratis offerte" + korte value prop

5. Privacy policy URL:
   └── Verplicht: link naar privacy page

6. Submit button text:
   └── "Versturen" of "Aanvragen"

7. Background image:
   └── Optioneel, verhoogt engagement

LEAD DELIVERY:
──────────────
□ Webhook naar CRM (real-time)
□ Email notificatie
□ Download CSV (manual)
□ Zapier integratie
```

### Lead Form Kwaliteit Verbeteren

```
LEAD FORM QUALITY OPTIMIZATION
══════════════════════════════

PROBLEEM: Slechte lead kwaliteit

OPLOSSINGEN:
────────────

1. VOEG QUALIFICATION VRAGEN TOE
   ├── Budget vraag filtert tire-kickers
   ├── Timing vraag prioriteert hot leads
   └── Specifieke needs vraag toont serieuze intent

2. GEBRUIK LANGERE FORM VARIANT
   └── Google biedt "More qualified" template

3. COMBINEER MET LANDING PAGE
   └── Lead form alleen voor mobile
   └── Desktop naar landing page

4. SNELLE FOLLOW-UP
   ├── Contact binnen 5 minuten = 21x hogere connect rate
   ├── Webhook naar sales team
   └── Auto-responder email

5. LEAD SCORING SETUP
   ├── Score op basis van antwoorden
   ├── High-score leads = priority
   └── Low-score leads = nurturing track
```

## Call Tracking Setup

### Google Ads Call Tracking

```
CALL TRACKING IMPLEMENTATIE
═══════════════════════════

OPTIE 1: GOOGLE ADS CALL TRACKING (Gratis)
──────────────────────────────────────────
Hoe het werkt:
├── Google vervangt je nummer met tracking nummer
├── Tracked calls als conversions
└── Beperkt: alleen clicks vanuit ads

Setup:
1. Tools → Conversions → New conversion → Phone calls
2. Kies: "Calls from ads using call extensions"
3. Stel call length threshold in (vaak 60+ sec)
4. Link aan campaigns

OPTIE 2: WEBSITE CALL CONVERSIONS
─────────────────────────────────
Tracked calls vanuit website (na ad click):
1. Tools → Conversions → "Calls from website"
2. Implementeer Google forwarding snippet
3. Dynamisch nummer replacement
4. Tracked in full customer journey

OPTIE 3: THIRD-PARTY CALL TRACKING
──────────────────────────────────
Tools: CallRail, CallTrackingMetrics, Infinity
Voordelen:
├── Call recording
├── Call scoring/qualification
├── Multi-channel attribution
├── CRM integratie
└── Keyword-level tracking

CALL CONVERSIE INSTELLINGEN:
────────────────────────────
□ Call length: 60+ seconden (lead gen)
□ Count: One (niet every)
□ Attribution: Data-driven (indien voldoende data)
□ Value: Gemiddelde lead waarde
```

## Google Ads Script: Lead Gen Campaign Checker

```javascript
/**
 * Lead Gen Campaign Health Check Script
 *
 * Controleert:
 * - Extension coverage
 * - Ad strength
 * - Conversion tracking
 * - Budget utilization
 *
 * Setup:
 * 1. Pas CONFIG aan
 * 2. Schedule: Wekelijks
 */

var CONFIG = {
  EMAIL: 'jouw@email.com',

  // Thresholds
  MIN_SITELINKS: 4,
  MIN_CALLOUTS: 4,
  MIN_AD_STRENGTH: 'GOOD', // POOR, AVERAGE, GOOD, EXCELLENT
  MIN_CONVERSION_RATE: 0.02, // 2%

  // Date range
  DATE_RANGE: 'LAST_30_DAYS'
};

function main() {
  var report = {
    campaigns: [],
    issues: [],
    recommendations: []
  };

  var campaigns = AdsApp.campaigns()
    .withCondition('Status = ENABLED')
    .withCondition('CampaignType = SEARCH')
    .get();

  while (campaigns.hasNext()) {
    var campaign = campaigns.next();
    var campaignReport = analyzeCampaign(campaign);
    report.campaigns.push(campaignReport);

    if (campaignReport.issues.length > 0) {
      report.issues = report.issues.concat(campaignReport.issues);
    }
  }

  // Generate recommendations
  report.recommendations = generateRecommendations(report);

  if (report.issues.length > 0) {
    sendReport(report);
  }

  Logger.log('Lead Gen Check Complete');
  Logger.log('Campaigns analyzed: ' + report.campaigns.length);
  Logger.log('Issues found: ' + report.issues.length);
}

function analyzeCampaign(campaign) {
  var name = campaign.getName();
  var issues = [];

  // Check Extensions
  var sitelinks = campaign.extensions().sitelinks().get();
  var sitelinkCount = 0;
  while (sitelinks.hasNext()) { sitelinks.next(); sitelinkCount++; }

  if (sitelinkCount < CONFIG.MIN_SITELINKS) {
    issues.push({
      campaign: name,
      type: 'EXTENSION',
      issue: 'Minder dan ' + CONFIG.MIN_SITELINKS + ' sitelinks (' + sitelinkCount + ')'
    });
  }

  var callouts = campaign.extensions().callouts().get();
  var calloutCount = 0;
  while (callouts.hasNext()) { callouts.next(); calloutCount++; }

  if (calloutCount < CONFIG.MIN_CALLOUTS) {
    issues.push({
      campaign: name,
      type: 'EXTENSION',
      issue: 'Minder dan ' + CONFIG.MIN_CALLOUTS + ' callouts (' + calloutCount + ')'
    });
  }

  // Check Call Extensions
  var hasCallExtension = false;
  try {
    var callExtensions = campaign.extensions().phoneNumbers().get();
    if (callExtensions.hasNext()) {
      hasCallExtension = true;
    }
  } catch (e) {}

  if (!hasCallExtension) {
    issues.push({
      campaign: name,
      type: 'EXTENSION',
      issue: 'Geen call extension actief'
    });
  }

  // Check Ad Strength per Ad Group
  var adGroups = campaign.adGroups()
    .withCondition('Status = ENABLED')
    .get();

  while (adGroups.hasNext()) {
    var adGroup = adGroups.next();
    var ads = adGroup.ads()
      .withCondition('Type = RESPONSIVE_SEARCH_AD')
      .withCondition('Status = ENABLED')
      .get();

    while (ads.hasNext()) {
      var ad = ads.next();
      var rsa = ad.asType().responsiveSearchAd();
      var strength = rsa.getAdStrength();

      if (isWeakAdStrength(strength)) {
        issues.push({
          campaign: name,
          adGroup: adGroup.getName(),
          type: 'AD_STRENGTH',
          issue: 'RSA ad strength is ' + strength
        });
      }
    }
  }

  // Check Conversion Rate
  var stats = campaign.getStatsFor(CONFIG.DATE_RANGE);
  var clicks = stats.getClicks();
  var conversions = stats.getConversions();

  if (clicks > 100) {
    var convRate = conversions / clicks;
    if (convRate < CONFIG.MIN_CONVERSION_RATE) {
      issues.push({
        campaign: name,
        type: 'PERFORMANCE',
        issue: 'Conversion rate ' + (convRate * 100).toFixed(2) +
               '% is onder target ' + (CONFIG.MIN_CONVERSION_RATE * 100) + '%'
      });
    }
  }

  return {
    name: name,
    sitelinks: sitelinkCount,
    callouts: calloutCount,
    hasCallExtension: hasCallExtension,
    conversions: conversions,
    clicks: clicks,
    issues: issues
  };
}

function isWeakAdStrength(strength) {
  var strengthOrder = ['POOR', 'AVERAGE', 'GOOD', 'EXCELLENT'];
  var minIndex = strengthOrder.indexOf(CONFIG.MIN_AD_STRENGTH);
  var currentIndex = strengthOrder.indexOf(strength);
  return currentIndex < minIndex;
}

function generateRecommendations(report) {
  var recommendations = [];

  var extensionIssues = report.issues.filter(function(i) {
    return i.type === 'EXTENSION';
  });

  if (extensionIssues.length > 0) {
    recommendations.push({
      priority: 'HIGH',
      action: 'Voeg ontbrekende extensions toe voor betere CTR en lead volume',
      campaigns: extensionIssues.length + ' campaigns affected'
    });
  }

  var adStrengthIssues = report.issues.filter(function(i) {
    return i.type === 'AD_STRENGTH';
  });

  if (adStrengthIssues.length > 0) {
    recommendations.push({
      priority: 'MEDIUM',
      action: 'Verbeter ad strength door meer headline/description variatie',
      campaigns: adStrengthIssues.length + ' ad groups affected'
    });
  }

  return recommendations;
}

function sendReport(report) {
  var subject = 'Lead Gen Campaign Check - ' + AdsApp.currentAccount().getName();
  var body = 'Lead Generation Campaign Health Check\n';
  body += '=====================================\n\n';

  body += 'CAMPAIGNS ANALYZED: ' + report.campaigns.length + '\n';
  body += 'ISSUES FOUND: ' + report.issues.length + '\n\n';

  if (report.issues.length > 0) {
    body += 'ISSUES:\n';
    body += '───────\n';

    for (var i = 0; i < report.issues.length; i++) {
      var issue = report.issues[i];
      body += '• ' + issue.campaign;
      if (issue.adGroup) body += ' / ' + issue.adGroup;
      body += '\n  ' + issue.issue + '\n\n';
    }
  }

  if (report.recommendations.length > 0) {
    body += 'RECOMMENDATIONS:\n';
    body += '────────────────\n';

    for (var j = 0; j < report.recommendations.length; j++) {
      var rec = report.recommendations[j];
      body += '[' + rec.priority + '] ' + rec.action + '\n';
      body += '  ' + rec.campaigns + '\n\n';
    }
  }

  body += '\n---\nGenerated by Lead Gen Campaign Checker Script';

  MailApp.sendEmail(CONFIG.EMAIL, subject, body);
  Logger.log('Report sent to ' + CONFIG.EMAIL);
}
```

## Output: Search Campaign Setup Template

```markdown
# Search Campaign Setup Plan

## Campaign Overzicht
- **Business type:** [B2B/B2C/Local Service]
- **Target locations:** [Locaties]
- **Monthly budget:** €[X]
- **Target CPL:** €[X]
- **Primary conversion:** [Form/Call/Lead Form]

## Campaign Structuur

### Campaign 1: [Naam]
**Type:** Search
**Bidding:** Target CPA / Maximize Conversions
**Daily budget:** €[X]

**Ad Groups:**
| Ad Group | Keywords (sample) | Match Types |
|----------|-------------------|-------------|
| [AG1] | [kw1], [kw2] | Exact, Phrase |
| [AG2] | [kw1], [kw2] | Phrase, Broad |

## RSA Setup

### Headlines (15):
1. [Keyword-focused headline - PIN position 1]
2. [Second keyword variation - PIN position 1]
3. [Primary benefit - PIN position 2]
4. [Social proof]
5-15. [Variaties op benefits, CTAs, USPs]

### Descriptions (4):
1. [Keyword + value prop + CTA - PIN position 1]
2. [Trust + process]
3. [USPs + social proof]
4. [Urgency + secondary CTA]

## Extensions Checklist
- [ ] 6+ Sitelinks configured
- [ ] 6+ Callouts added
- [ ] Structured Snippets (2 types)
- [ ] Call Extension active
- [ ] Location Extension linked
- [ ] Lead Form Extension (optional)
- [ ] Image Extensions added

## Conversion Tracking
- [ ] Form submission tracking
- [ ] Call tracking enabled (60+ sec)
- [ ] Lead form webhook configured
- [ ] Value per conversion set

## Launch Checklist
- [ ] All ads reviewed for spelling/accuracy
- [ ] Landing pages mobile-optimized
- [ ] Negative keyword lists applied
- [ ] Budget and bids confirmed
- [ ] Conversion tracking verified
```
